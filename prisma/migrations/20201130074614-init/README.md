# Migration `20201130074614-init`

This migration has been generated by pavarit at 11/30/2020, 2:46:14 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "Teacher" (
"id" SERIAL,
    "email" TEXT NOT NULL,
    "first_name" TEXT,
    "last_name" TEXT,
    "date_of_birth" TIMESTAMP(3) NOT NULL,
    "age" TEXT,
    "country" TEXT,

    PRIMARY KEY ("id")
)

CREATE TABLE "Course" (
"id" SERIAL,
    "course_name" TEXT NOT NULL,
    "course_description" TEXT,
    "course_length" INTEGER,
    "language" TEXT,
    "teacherId" INTEGER,

    PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "Teacher.email_unique" ON "Teacher"("email")

ALTER TABLE "Course" ADD FOREIGN KEY("teacherId")REFERENCES "Teacher"("id") ON DELETE SET NULL ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201130074614-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,32 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Teacher {
+  id            Int     @default(autoincrement()) @id
+  email         String  @unique
+  first_name    String?
+  last_name     String?
+  date_of_birth DateTime
+  age           String?
+  country       String?
+  course        Course[]
+}
+
+model Course {
+  id                   Int     @default(autoincrement()) @id
+  course_name          String
+  course_description   String?
+  course_length        Int?
+  language             String?
+  teacher              Teacher?   @relation(fields: [teacherId], references: [id])
+  teacherId            Int?
+}
```


